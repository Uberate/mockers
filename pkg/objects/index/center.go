package index

// GetFunc will return the Indexable function by specify name and version. When GetFunc can't find the specify function,
// It will return an error.
func GetFunc(name, version string) (Indexable, error) {

}

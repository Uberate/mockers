# The cluster mode define

## Backup cluster.

The backup cluster, all data will be store in different nodes. All the nodes has all data.

The object-mocker can start with config: `BackupNodes` param, to set the `backup` mode.
